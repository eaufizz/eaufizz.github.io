import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asobihirameki.molgraph',
  appName: 'molgraph',
  webDir: 'dist/molkky-app/browser'
};

export default config;
